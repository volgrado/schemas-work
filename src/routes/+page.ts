// src/routes/+page.ts
// This tells SvelteKit to prerender the root page, generating a static index.html
// at the root of your site, which is necessary for the client-side redirect to work.
export const prerender = true;

import type { SchemaMetadata } from '$lib/types';

/**
 * Devuelve una lista con los metadatos de todos los esquemas del usuario.
 */
export async function listSchemas(): Promise<SchemaMetadata[]> {
  console.log('TODO: Implementar listSchemas');
  return Promise.resolve([]); // Devuelve un array vacío por ahora
}